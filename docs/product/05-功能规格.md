# 05 · 功能规格

> 状态:**骨架**。先列出功能模块清单,每块一句话;细节随讨论逐步填充。

## 功能模块清单

### A. 学习目标管理
- 创建学习目标(一句话 / 上传文档两种入口);
- 查看 / 编辑 / 归档目标。

`(待深化)`

### B. 材料管理
- 上传文档,查看解析 / 向量化状态;
- 材料关联到学习目标。

`(待深化)`

### C. 学习路线
- 展示 agent 生成的阶段与小目标;
- 标识当前进度;
- 是否允许用户调整路线 `(待定,见 07 Q6)`。

`(待深化)`

### D. 陪练教学(核心交互)
- 聊天式陪练界面,流式输出;
- 单节流程:诊断 → 讲解 → 练习 → 验证。

`(待深化)`

### E. 学习验证 / 练习
- 解释、选择题、填空、粘贴运行结果、提交代码片段;
- agent 判定掌握层级。

`(待深化)`

### F. 学习画像
- 展示掌握程度、已完成主题、薄弱点、下一步;
- 是否允许用户查看 / 修改 `(待定,见 07 Q6)`。

`(待深化)`

### G. 学习日志
- 每日学习记录;
- 自动写入还是用户确认后写入 `(待定,见 07 Q5)`。

`(待深化)`

### H. 账户 / 订阅 / 设置
- 登录(复用现有 JWT);
- 模型 / API key 配置(BYOK);
- 订阅与用量 `(商业化阶段)`。

`(待深化)`
